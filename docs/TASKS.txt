RAW IDEA BACKLOG — not a plan, not sequenced, not costed.

Kept verbatim because it is where several things came from: the five unbuilt
control screens (purchasing, service, store, logistics, and the two floor
dashboards) trace to the list below, as does the analytics ambition.

Nothing here is scheduled. What is actually next, verified against the code, is
in ../HANDOFF.md §3 and docs/DESIGN-KIT.md. When an item here becomes real work
it should move there and be struck from this file, or the two will disagree.

================================================================================

Check that we have the same full menu items list on all pages in design kit. Sync that.

Taking into account all prices, spendings, tarifs, discounts, etc. create/develop an web app (page/tab in Printorian) - best in class analitical platfrom to investigate and get the best/most valueable business model to calculate/optimize the highest revenew and the lowest/concurent price for customers. We should get a lot of plots of different numerical experiments and different developed optimization instruments and algorithms.

Fill database with full imaginary data types with different parapemeters to evaluate correct view/calculation/etc.

Pages:
 - postproduction manager/operator dashboard, workflow, tasks, instructions and accumulated/overall analitical markers/indicators/badges/evaluations
 - the same for packaging
 - our buyings (materials, spare parts, packaging, new printers, etc.) with order detailes and accumulated/overall analitical markers/indicators/badgets/evaluations
 - service dashboard, statuses, tasks management (printers installation (new) and repair, service, materials loading, models movement, etc.)
 - store management
 - logistics management

Analize the current database architecture, structure, interconnections, it's solidity, maturity and future proof. Make the resulting report and recomendations of changes and tasks. I do not whant to change database on the late developemnt stage - it will be too complicated. Take into account design kit and docs - what have to be implemented as a whole. Evaluate and take into account scalability of database as the farm growth. May be to devide it but not to looze speed. May be to implement/fix something to increase speed. Take into account possible backup/reservation/restore.

--------------

We should provide intellectual service/algorithms/advice to decrease the price of created and shiped model (order).

Does 3mf file contain all instructions and printers setup parameterts? Does the file structure is standartized? Can we create it manually? Can we set parameters that harcoded (in definite list items) in Bambu Studio (ex., layer height, speed, etc.)?

I want to create AI agent(s):
- model creation, optimization and slicing
- printers and printing parameters setup and optimizations
- clients and orders management
- financial management
- printers and production management (service, workflow, etc)
- product management (model, print order, postproduction, packaging)
- logistics management
- etc
Research, develop and describe in detaile the procedure with most efficient hardware/software/technology setup to solve these tasks with agents creation and training. I want to use (NVIDIA V100 16G) x 4 NPU throught PCI-E on PC workstation or any other low/mid budget hardware solutions. Use existing solutions (models, libraries) as much as possible to solve task faster. What data should i get/gether and how? What existing models i can use (hugginface, etc)? Budget is limited to 2000-3000$.