# The storefront, if it is reachable from the shop-floor LAN. Same rule as the
# storefront's VITE_CONSOLE_URL: unset means the overlay lists no ВИТРИНА
# territory at all, rather than listing a link that fails.
# VITE_STOREFRONT_URL=http://localhost:5173
